# Empreinte (hash) d'un dataset, pour tracer la provenance d'un rapport

Empreinte (hash) d'un dataset, pour tracer la provenance d'un rapport

## Usage

``` r
dataset_fingerprint(dataset)
```

## Arguments

- dataset:

  Un `me_dataset`.

## Value

Chaine de caracteres (hash SHA-1 tronque).
