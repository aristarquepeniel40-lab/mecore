% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_metadata.R
\name{me_metadata}
\alias{me_metadata}
\title{Metadonnees d'un projet MEverse}
\usage{
me_metadata(
  project_name = character(0),
  organization = character(0),
  country = character(0),
  donor = character(0),
  manager = character(0),
  start_date = (function (.data = double()) 
 {
     .Date(.data)
 })(),
  end_date = (function (.data = double()) 
 {
     .Date(.data)
 })(),
  version = character(0),
  description = character(0),
  objectives = character(0),
  sdgs = character(0)
)
}
\arguments{
\item{project_name}{Nom du projet.}

\item{organization}{Organisation porteuse (ex. "Ministere de la Sante").}

\item{country}{Pays de mise en oeuvre.}

\item{donor}{Bailleur (ex. "Banque mondiale").}

\item{manager}{Responsable du projet.}

\item{start_date}{Date de debut (classe R \code{Date}).}

\item{end_date}{Date de fin (classe R \code{Date}).}

\item{version}{Version du projet/document (ex. "1.0").}

\item{description}{Description libre du projet.}

\item{objectives}{Objectifs du projet (vecteur de caracteres).}

\item{sdgs}{Codes ODD associes (ex. c("1.1.1", "4.1.1")).}
}
\description{
Decrit un projet AVANT toute donnee, indicateur ou rapport : nom,
organisation porteuse, pays, bailleur, responsable, dates, version,
description, objectifs.
}
\details{
Principe de conception (choix explicite) : ce fichier decrit UNIQUEMENT
la structure d'un \code{me_metadata}. Il ne contient aucune logique metier,
aucun calcul, aucune lecture de fichier, aucune validation au-dela du
typage natif de S7 (\code{properties}). Toute coherence a verifier entre
champs (ex. \code{end_date} >= \code{start_date}) est volontairement laissee a
une fonction separee, hors de la classe (voir ARCHITECTURE.md §2.4).
}
