% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_indicator.R
\name{me_indicator}
\alias{me_indicator}
\title{Indicateur MEverse}
\usage{
me_indicator(
  label = character(0),
  formula = (function (.data) 
 {
    
    stop(sprintf("S3 class <\%s> doesn't have a constructor", class[[1]]), call. =
    FALSE)
 })(),
  datasets = list(),
  value = NULL,
  unit = character(0)
)
}
\arguments{
\item{label}{Libelle lisible (ex. "Age moyen des enquetes").}

\item{formula}{Formule R (objet \code{formula}) definissant le calcul.}

\item{datasets}{Liste de \code{me_dataset} sources (1 ou plus).}

\item{value}{Valeur calculee (NULL tant que non calculee).}

\item{unit}{Unite de mesure (ex. "annees", "\%"). Peut etre vide si \code{value} non numerique.}
}
\description{
Definition + valeur calculee d'un indicateur. \code{mecore} ne definit que
la structure ; le calcul reel appartient a \code{meindicator}.
}
\details{
Invariant : un indicateur reference toujours au moins un \code{me_dataset}
source (interdiction de l'indicateur "orphelin", cf. ARCHITECTURE.md §2.2).
}
